<h2>退会お手続き</h2>
 <p>
   退会手続きをされますと、全てのサービスがご利用いただけなくなります。<br>
   再度ご利用をいただく場合には、新規登録のお手続きが必要になります。<br>
   本当に退会してよろしいですか？
 </p>
  <div class="col-2">
        <%= form_with model: @user, url: user_withdrawal_path, method: :patch do |f| %>
          <%= f.hidden_field :is_deleted, value: true %>
          <%= f.submit "退会する", class: "btn btn-danger", data: { confilm: "本当に退会しますか？" } %>
      <% end %>
      </div>
  <%= link_to "退会する", user_withdrawal_path, method: :patch, data: { confilm: "本当に退会しますか？" } %>
  <%#= link_to "退会しない", user_path(current_user) %>
  <%#= button_to "退会する", root_path, method: :get, id: 'withdrawal' %>
  <%#= link_to '退会', user_withdrawal_path, data: { turbo_method: :delete, turbo_confirm: 'OK?' } %>  
 <script>
  let btn = document.getElementById('user_withdrawal');
  btn.addEventListener('click', function() {
    result = window.confirm("確認ダイアログ")
    
    if(result) {
     
    } else {
     
    }
  })
 </script>